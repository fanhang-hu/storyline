# LaTeX Templates

Place the target conference or journal LaTeX template in this directory before using the `template-latex-export` skill.

Expected contents may include:

- the main `.tex` template file
- `.cls`, `.sty`, or `.bst` files
- template images or logos
- sample `.bib` files

Generated LaTeX output should be written to `target/latex/`, not back into this template directory.
